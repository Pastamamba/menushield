{
  "name": "menu-shield-backend",
  "type": "module",
  "scripts": {
    "start": "npx prisma db push && node prisma/seed-safe.js && node server.js",
    "dev": "node --watch server.js",
    "build": "npx prisma generate",
    "migrate:deploy": "npx prisma db push",
    "generate": "npx prisma generate",
    "db:seed": "node prisma/seed-mongodb-fixed.js"
  },
  "dependencies": {
    "@paralleldrive/cuid2": "^2.2.2",
    "@prisma/client": "^5.15.0",
    "bcryptjs": "^2.4.3",
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^5.1.0",
    "express-rate-limit": "^8.1.0",
    "express-validator": "^7.2.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "prisma": "^5.15.0"
  }
}
